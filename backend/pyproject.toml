[project]
name = "gateway"
version = "0.1.0"
description = "Python project to be bridge of the main app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
    "zeroconf>=0.148.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
]
